apiVersion: v1
kind: ServiceAccount
metadata:
  name: server-ingest
  namespace: identity-bridge
  labels:
    app: server-ingest
    tier: ingestion
  # Uncomment for MSK IAM auth (alternative to SCRAM Secret):
  # annotations:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/identity-bridge-server-ingest-msk
