apiVersion: v1
kind: Namespace
metadata:
  name: identity-bridge
  labels:
    app.kubernetes.io/part-of: identity-bridge
    app.kubernetes.io/component: platform
    tier: ingestion
    environment: staging   # override per env: dev | staging | prod
